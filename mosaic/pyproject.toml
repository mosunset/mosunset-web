[project]
name = "mosaic"
version = "0.3.0"
description = "顔、名前、日付を自動検出してモザイク処理するツール（GUI/CLI両対応）"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "opencv-python>=4.12.0.88",
    "easyocr>=1.7.0",
    "numpy>=1.24.0",
    "pillow>=10.0.0",
]
