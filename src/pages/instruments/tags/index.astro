---
import Layout from "@/layouts/Layout.astro";
import InstrumentCard from "@/components/Card/InstrumentCard.astro";
import TagsListPage from "@/components/Pages/Tags/TagsListPage.astro";
import {
    getPublicPosts,
    getPostsByTagSorted,
    SECTION_CONFIGS,
} from "@/components/Pages/utils";

const config = SECTION_CONFIGS.instruments;

// コレクションから公開記事を取得
const publicPosts = await getPublicPosts(config.collectionName);

// タグごとにグループ化してソート
const postsByTag = getPostsByTagSorted(publicPosts, config.sortField);
---

<Layout title="タグ - 楽器一覧">
    <TagsListPage
        postsByTag={postsByTag}
        sectionName={config.sectionName}
        borderColor="border-green-200"
        CardComponent={InstrumentCard}
    />

</Layout>
