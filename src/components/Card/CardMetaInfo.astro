---
export interface Props {
    label: string;
    value: string;
    transitionName: string;
}

const { label, value, transitionName } = Astro.props;
---

<p
    class="text-gray-600 mb-2 text-sm"
    transition:persist-props
    transition:name={transitionName}
>
    {label}: {value}
</p>
